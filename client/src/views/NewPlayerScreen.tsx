import {FC, useEffect} from "react";
import httpClient from "../http-client";

export const NewPlayerScreen: FC = () => {
    // useEffect(() => {
    //
    // }, [])

    return (
        <div>
            new player
        </div>
    )
}